//! Port of `compiler-dom/src/transforms/stringifyStatic.ts` (pending).

use crate::core::ast::NodeId;
use crate::core::transform::TransformContext;

pub fn stringify_static(_node: NodeId, _ctx: &mut TransformContext) {}
